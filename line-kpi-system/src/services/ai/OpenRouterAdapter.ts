// Kept for backward compatibility — logic has moved to UniversalAdapter
export { UniversalAdapter as OpenRouterAdapter } from './UniversalAdapter';
export type { UniversalAdapterConfig as OpenRouterAdapterConfig } from './UniversalAdapter';
