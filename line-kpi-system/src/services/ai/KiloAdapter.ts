// Kept for backward compatibility — logic has moved to UniversalAdapter
export { UniversalAdapter as KiloAdapter } from './UniversalAdapter';
export type { UniversalAdapterConfig as KiloAdapterConfig } from './UniversalAdapter';
